Induced subgraph isomorphism in GHC-Haskell

Code in public domain.

To use:

1. Make sure `ghc` is directly invokable from command line.

2. `make && cd graphs`

3. `../bin/isub C6 cube` to see an induced C6 in the cube.
   `../bin/tikz triangle` to generate tikzcode that draws the triangle.
